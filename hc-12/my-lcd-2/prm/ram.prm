
NAMES
END

SECTIONS
    MY_RAM        = READ_WRITE 0x1000 TO 0x17FF;
    MY_PSEUDO_ROM = READ_ONLY  0x1800 TO 0x3BFF; 
END
    
PLACEMENT
    _PRESTART, STARTUP,
    ROM_VAR, STRINGS,
    NON_BANKED,DEFAULT_ROM,
    COPY                   INTO   MY_PSEUDO_ROM;
    DEFAULT_RAM            INTO   MY_RAM;
END

STACKSIZE 0x100

/* VECTOR 0  _Startup */

